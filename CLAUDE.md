@AGENTS.md

# Family Day Planner — правила ядра

Планувальник дня для перевантажених мам. Продукт: `PRODUCT_SPEC_v2.md`.
Порядок білду: `BUILD_PLAN.md`. Контракт даних: `docs/CONTRACT.md`.

## Незмінні архітектурні рішення

- **LLM тільки парсить текст у JSON. LLM НЕ розкладає час і НЕ оцінює тривалості.**
- **Розкладку робить детермінований solver** — проста функція з циклами й перевірками.
  Без зовнішніх solver-бібліотек, без оптимізації. Мета — притомний день, не оптимальний.
- **Тривалості призначає solver** зі словника дефолтів (+ fallback 30 хв).
- Вивід LLM валідуємо схемою, безпечний парсинг, один ретрай, потім чесна помилка.
  **Ніякої другої моделі** для перевірки виводу.
- LLM-ключ тільки на сервері (`/api/plan`), ніколи на клієнті. Env: `GEMINI_API_KEY`.

## Дисципліна роботи

- **Будуємо строго поетапно за BUILD_PLAN.** Зараз — тільки Етап 1.
  Не додавати памʼять (Етап 2), Telegram (Етап 3), шеринг (4), дизайн (5).
- Бачиш можливість «заодно додати» щось з пізніших етапів → **не додавай, спитай**.
- **Нуль дизайну до Етапу 5.** Системний шрифт, чорний текст, білий фон.
- Кожен крок — окремий commit з осмисленим меседжем.
- Solver — з юніт-тестами (його головна перевага).

## Стек

Next.js (App Router) + TypeScript, деплой на Vercel. LLM: Google AI Studio (Gemini),
SDK `@google/genai` (підключення на кроці 1.3).
